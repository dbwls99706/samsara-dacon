# SAMSARA · 윤회 — 환경변수 예시
# 이 파일을 `.env.local` 로 복사 후 실제 값 입력. .env.local 은 git ignore 됨.

# Supabase 익명 리더보드 (없으면 로컬 랭킹만 동작)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
